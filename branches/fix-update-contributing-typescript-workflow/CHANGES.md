# Changes for fix/update-contributing-typescript-workflow

## Summary
Align `CONTRIBUTING.md` with the active TypeScript/NestJS/Angular repository workflow so new contributors follow the current install, startup, and validation paths.

## Changes
- Replaced the stale Python/Poetry prerequisites and installation flow with the current Node.js/npm root workspace setup.
- Updated contribution guidance to reference the active Angular UI, NestJS backend, and shared SDK package structure.
- Added root and workspace validation commands that match the current repository scripts.
